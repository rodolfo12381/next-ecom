import React from 'react'

export default function Contactlayout({
    children
}: {
    children: React.ReactNode;
}) {
  return (
    <div>
        <div>
            Contact Layout
        </div>
        {children}
    </div>
  )
}
